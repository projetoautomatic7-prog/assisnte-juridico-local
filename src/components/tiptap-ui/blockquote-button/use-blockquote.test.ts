import { describe, it, expect } from "vitest";

describe("useBlockquote hook", () => {
  it("creates blockquote", () => {
    expect(true).toBe(true);
  });

  it("toggles blockquote", () => {
    expect(true).toBe(true);
  });

  it("checks if blockquote is active", () => {
    expect(true).toBe(true);
  });
});
