import { describe, it, expect } from "vitest";

describe("BlockquoteButton", () => {
  it("creates blockquote", () => {
    expect(true).toBe(true);
  });

  it("toggles blockquote formatting", () => {
    expect(true).toBe(true);
  });

  it("removes blockquote formatting", () => {
    expect(true).toBe(true);
  });
});
