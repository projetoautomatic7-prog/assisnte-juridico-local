import { describe, it, expect } from "vitest";

describe("useMark hook", () => {
  it("applies mark formatting", () => {
    expect(true).toBe(true);
  });

  it("removes mark formatting", () => {
    expect(true).toBe(true);
  });

  it("toggles mark formatting", () => {
    expect(true).toBe(true);
  });

  it("checks if mark is active", () => {
    expect(true).toBe(true);
  });
});
