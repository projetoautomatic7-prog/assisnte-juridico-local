import { describe, it, expect } from "vitest";

describe("ImageUploadButton", () => {
  it("opens file picker", () => {
    expect(true).toBe(true);
  });

  it("uploads image to editor", () => {
    expect(true).toBe(true);
  });

  it("displays image with alt text", () => {
    expect(true).toBe(true);
  });

  it("resizes image", () => {
    expect(true).toBe(true);
  });

  it("removes image from editor", () => {
    expect(true).toBe(true);
  });
});
